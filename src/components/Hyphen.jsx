import React from 'react'
import { primaryColor } from '../constants'

const Hyphen = () => {
    return (
        <span style={{width:20, height: 2, marginBottom: 5, marginRight: 10, backgroundColor:primaryColor, display:"inline-block"}} />
    )
}

export default Hyphen
